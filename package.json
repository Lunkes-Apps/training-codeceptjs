{
  "name": "testblz",
  "version": "1.0.0",
  "description": "Test to automacaocombatista",
  "main": "AccessTestPage_test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "codeceptjs": "codeceptjs run --features --reporter mochawesome",
    "generateSteps": "codeceptjs gherkin:snippets",
    "start-docker": "docker run -p 4040:4040 -p 5050:5050 -e CHECK_RESULTS_EVERY_SECONDS=3 -e KEEP_HISTORY='TRUE' -v ${PWD}/output:/app/allure-results frankescobar/allure-docker-service",
    "codeceptJs-report":"codeceptjs run --features --reporter mochawesome --plugins allure"
  },
  "author": "Alexandre Lunkes",
  "license": "ISC",
  "dependencies": {
    "codeceptjs": "^2.3.0",
    "mochawesome": "^4.1.0"
  },
  "devDependencies": {
    "mocha": "^6.2.0"
  }
}
